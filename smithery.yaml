startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      onesourceBaseUrl:
        type: string
        default: "https://skills.onesource.io"
        description: "OneSource API base URL"
      analytics:
        type: boolean
        default: true
        description: "Enable usage analytics"
      analyticsUrl:
        type: string
        description: "Analytics dashboard URL (e.g. https://1s-analytics.vercel.app)"
      analyticsKey:
        type: string
        description: "API key for the analytics dashboard"
      x402PrivateKey:
        type: string
        description: "EVM private key (hex, 0x-prefixed) for automatic x402 USDC payments on Base"
  commandFunction:
    |-
    (config) => ({
      command: 'npx',
      args: ['-y', '@one-source/mcp'],
      env: {
        ONESOURCE_BASE_URL: config.onesourceBaseUrl || 'https://skills.onesource.io',
        ONESOURCE_ANALYTICS: config.analytics === false ? 'false' : 'true',
        ...(config.analyticsUrl ? { ONESOURCE_ANALYTICS_URL: config.analyticsUrl } : {}),
        ...(config.analyticsKey ? { X402_ANALYTICS_KEY: config.analyticsKey } : {}),
        ...(config.x402PrivateKey ? { X402_PRIVATE_KEY: config.x402PrivateKey } : {})
      }
    })
