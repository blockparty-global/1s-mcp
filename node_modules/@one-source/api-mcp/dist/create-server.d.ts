/**
 * MCP Server Factory
 *
 * Creates a configured McpServer with all OneSource API tools.
 * Shared by stdio (cli.ts) and HTTP modes.
 */
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { OneSourceClient } from './client.js';
import { type Analytics } from './analytics.js';
export declare const VERSION: string;
export interface CreateServerOptions {
    /** Override the default client (useful for testing). */
    client?: OneSourceClient;
    /** Override the default analytics (useful for testing). */
    analytics?: Analytics;
    /** Transport mode — passed through to analytics events. */
    transport?: 'stdio' | 'http';
}
/**
 * Create a configured MCP server with all OneSource API tools.
 */
export declare function createMcpServer(opts?: CreateServerOptions): {
    server: McpServer;
    analytics: Analytics;
};
export type CreateServerResult = ReturnType<typeof createMcpServer>;
export { StreamableHTTPServerTransport } from '@modelcontextprotocol/sdk/server/streamableHttp.js';
//# sourceMappingURL=create-server.d.ts.map