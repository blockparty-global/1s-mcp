import { networkParam, addressParam, formatResponse, safePath } from '../../types.js';
export const tool = {
    name: '1s_contract_code',
    description: 'Get the deployed bytecode of a contract via eth_getCode. ' +
        "Returns '0x' for EOA (non-contract) addresses. " +
        'Bytecode can be very large for complex contracts. ' +
        'Prefer 1s_proxy_detect or 1s_contract_info_live for type checks without fetching full bytecode.',
    category: 'chain',
    schema: {
        address: addressParam,
        network: networkParam,
    },
    handler: async (input, client) => {
        const res = await client.get(`/api/chain/code/${safePath(input.address)}`, {
            network: input.network,
        });
        return formatResponse(res);
    },
};
//# sourceMappingURL=contract-code.js.map